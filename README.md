# Operator.Station

`PiSubmarine.Operator.Station` is the native operator-side application module.

This initial skeleton provides the standard PiSubmarine module layout with:

- a reusable library target
- an application target
- a test target
- a mock target

The first implementation is intentionally minimal so the module can evolve from
an agreed architecture rather than from placeholder UI code.
